---
description: Push changes to GitHub with clean merge to main
---

## Steps

1. Stage all changes and commit with a descriptive message
// turbo
2. Push the current branch to origin
3. Checkout main, merge the feature branch with `--no-ff`, and push main
4. Checkout back to the working branch `gregoryLara`
